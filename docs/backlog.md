# Бэклог — elkhovering.com

Перенесён из `BACKLOG.xlsx` 2026-09-03. Оригинал удалён: единственный источник
правды теперь этот файл. Достать исходник, если понадобится:
`git show 08f0899:BACKLOG.xlsx > BACKLOG.xlsx`

**Обозначения.** Приоритет: 🔴 высокий · 🟡 средний · ⚪ низкий.
Объём: S — часы, M — день, L — несколько дней.

---

## Блокеры — нужно действие от Жени

| ID | Задача | Что именно сделать |
|---|---|---|
| **B-24** 🔴 | CI не может писать в пакет ghcr | Пакет создан приватным и не привязан к репозиторию, потому что первый образ залит руками. `GITHUB_TOKEN` в такой писать не может. Открыть [настройки пакета](https://github.com/users/Elkhovering/packages/container/elkhovering-com/settings) → *Danger Zone* → удалить пакет, затем перезапустить workflow: он пересоздаст его привязанным и публичным. Либо *Manage Actions access* → добавить репозиторий с ролью **Write**. |
| **B-25** 🔴 | Секреты мёртвого хостинга живы | В репозитории висят `SSH_PRIVATE_KEY`, `SSH_PASSPHRASE`, `SSH_USER`, `FTP_HOST`, `FTP_USERNAME`, `FTP_PASSWORD`. Новый workflow не использует ни один. Удалить из GitHub **и отозвать на стороне Namecheap** — живой приватный ключ и FTP-пароль от отключённого аккаунта это риск. |
| **B-21** 🟡 | Почта на своём домене | Включить iCloud+ Custom Email Domain, прописать MX/SPF/DKIM в Cloudflare, отключить Email Routing. Подробности в [plan.md](plan.md#почта). |
| **B-29** ⚪ | Кеш 3D-модели на Cloudflare | `.glb` не входит в расширения, которые Cloudflare кеширует по умолчанию, — модель каждый раз идёт с сервера через туннель. Cloudflare → домен → *Caching* → *Cache Rules* → новое правило: *URI Path* starts with `/_astro/` → *Eligible for cache*, *Edge TTL* — respect origin. Файлы там хешированы, отдаются с `immutable`, так что устареть нечему. |

---

## В работе

| ID | Приор. | Объём | Задача | Состояние |
|---|---|---|---|---|
| **B-01** | 🔴 | L | Кейсы вместо карточек проектов | Каркас и вёрстка готовы для всех 9 проектов EN+RU, `/works/[slug]`, morph-переход с обложки. Banana Bank сделан отдельным bespoke-компонентом и сейчас скрыт (`draft: true`). Осталось — реальный контент, см. B-19. |
| **B-19** | 🔴 | L | Реальный контент в кейсы | Сейчас в `src/content/works/{en,ru}/*.md` — заполнители. Нужно: сверить годы/роли/стек, добавить настоящие цитаты клиентов, метрики результата, галереи, живые ссылки. Делать по одному проекту, недоделанные держать под `draft: true`. |
| **B-22** | 🟡 | L | Блог + кросспостинг в Telegram | Спланирован, см. [plan.md](plan.md#блог). Нужно решение по направлению публикации. |

---

## Запланировано

| ID | Приор. | Объём | Задача | Заметки |
|---|---|---|---|---|
| **B-28** | 🔴 | M | Облегчить обложки на главной | **Ленивая загрузка сделана** и выкачена: размеры из файлов при сборке, вёрстка сверена попиксельно. **Осталось — сами файлы:** 8 PNG весят 18 МБ, из них `dotaboom/main.png` — 10,3 МБ при 5032×2628, хотя на экране он не шире 570 px. Перекодировать в WebP/AVIF с `srcset` под реальные ширины. Качество должен принять Женя — это витрина. Заодно `syrnik/main.png` всего 439×255 и на ретине мылится. |
| **B-31** | 🟡 | S | Слой медиа в образе меняется на каждой сборке | Dockerfile кладёт медиа отдельным слоем, чтобы правка текста не заливала 78 МБ заново. Но Astro при сборке копирует `public/` с новыми датами файлов, у слоя каждый раз новый хеш — и при выкате `c642f9b` сервер снова качал все 78 МБ, хотя ни одна картинка не менялась. Чинится обнулением дат в сборочном стейдже перед `COPY` (`find /dist-media -exec touch -h -d @0 {} +`) — проверить, что хеш слоя совпадает у двух сборок подряд. |
| **B-16** | 🔴 | M | Передизайн Контактов под Telegram | Сейчас композиция исходная: большой H1 — копируемый email, мелкий ряд — or/telegram/linkedin/behance. По позиционированию первичное действие — Telegram. Нужен блок, где Telegram главный, но композиция остаётся дизайнерской. Прототипировать: (а) большой `@elkhovering` + email вторичным, (б) split-H1, (в) асимметричные две колонки. Сначала дизайн, потом вёрстка. |
| **B-07** | 🔴 | M | Блок отзывов | Взять 2–3 коротких цитаты из трёх рекомендаций в CV (Мастер рекламы, Р-Групп, Nut Valley Armenia). Поставить между Works и Services либо между Services и Contacts. |
| **B-09** | 🟡 | S | Пустые ссылки LinkedIn / Behance | `src/components/Contacts.astro:18,20` — `href=""`, мёртвые клики. Либо убрать, либо проставить реальные профили. |
| **B-06** | 🟡 | M | Сократить услуги с 6 до 2–3 | Текущий список (сайты / бренд / NFT / консалтинг / тимлид / лекции) — это три разных бизнеса. При зафиксированном позиционировании свести к основному офферу (fractional digital lead) плюс один-два смежных. |
| **B-03** | 🟡 | M | OG-картинка и соцмета | Сгенерировать превью 1200×630 с лосём и вордмаркой, добавить `og:*` и `twitter:card` в Layout. Влияет на то, как ссылка выглядит в Telegram и LinkedIn — а с блогом это станет заметно важнее. |
| **B-08** | 🟡 | S | Ряд логотипов клиентов | 5–6 монохромных логотипов даёт мгновенное доверие. Обычно над сгибом или в About. |
| **B-15** | ⚪ | S | Лёгкая аналитика | Plausible или Umami — приватные, без баннера про куки. Просто чтобы знать, какие работы кликают. Не GA. |
| **B-11** | ⚪ | S | Убрать легаси-файлы из корня | `index.html`, `style.css`, `mobile.css`, `script.js`, `nav.html`, `works.html`, `en.json`, `ru.json`, `logo-06.png`. Уже исключены из docker-контекста, но в репозитории лежат. Два из них — облачные заглушки OneDrive, на которых подвисает git. |
| **B-12** | ⚪ | M | Форма обратной связи | Для тех, кому привычнее email. Formspree или аналог. Низкий приоритет: первичный канал — Telegram. |
| **B-23** | ⚪ | L | Своя админка вместо Sveltia | Обсуждалась архитектура: Cloudflare Workers + D1 + R2, всё пишется с нуля. Держать как направление, начинать не раньше, чем стабилизируется блог. |

---

## Сделано

| ID | Задача | Итог |
|---|---|---|
| **B-30** | Обложка проекта на лосе | При наведении на работу на главной тело лося становится гранёным камнем в цветах её обложки (обложка как matcap под светом тела), а рога и копыта подкрашиваются цветом размытого фона за ними. Выбрано вживую из пяти вариантов. Заменила прежний `mix-blend-mode: overlay`. Выкачено 2026-09-16. Устройство — в [status.md](status.md#3d-лось-и-прелоадер). |
| **B-02** | Spline → чистый three.js | Лось рендерится напрямую из `Brand/3D/los_anime1.glb`: матовые matcap-материалы, анимация, поворот по скроллу и взгляд за курсором воспроизведены по замерам рантайма Spline. Вес 3D на главной ~1,13 МБ → ~265 КБ (gzip), на остальных страницах three.js не грузится вовсе. Выкачено 2026-09-16. Устройство — в [status.md](status.md#3d-лось-и-прелоадер). |
| **B-26** | Прелоадер главной | «Разобранный чертёж» логотипа: детали знака садятся на место по реальным вехам загрузки сцены, затем знак перелетает к голове 3D-лося и страница раскрывается оттуда. Раз за сессию, с reduced motion, таймаутом и отказоустойчивостью. Выбран из трёх прототипов. Выкачено 2026-09-16. |
| **B-27** | Подпись при наведении на работы | Регрессия из `96a72a7`: ключи `projects.*` убраны из i18n, а главная всё ещё читала их — подпись при наведении пропала. Теперь название, описание и год берутся из тех же `.md`, что и кейсы, — правка в `/admin` меняет оба места. Плитка без опубликованного кейса больше не показывается на главной. Выкачено 2026-09-16. |
| **B-20** | Переезд на свой сервер | Апекс открыт, `elkhovering.com` опубликован через туннель Oracelk, сайт доступен публично. |
| **B-04** | Хедлайн под новое позиционирование | H1 → «Creative Developer» / «Креативный разработчик» (2026-09-12), подзаголовок → «The art director you hire before you need a team». Фраза «one person» осталась в About и в meta-описании — там она про модель работы, а не про должность. |
| **B-05** | About как манифест | Переписан по CV: стык IT/продукта/digital, 300+ проектов, «вхожу в хаос — оставляю платформу». Личное: экономические стратегии, садоводство, гейм-дизайн в Syrnik.dev. |
| **B-13** | Схема данных проекта | `src/content.config.ts`: slug/title/summary/year/role/url/client/stack/tags/cover/gallery/draft. Layout-мета (col/row/rellax/layer) осталась в `src/data/works.js` — это до сих пор второй источник правды, см. заметку в plan.md. |
| **B-18** | Единая стратегия i18n | Клиентская, через `html[data-lang]`. EN и RU сосуществуют в каждой странице, CSS прячет неактивную локаль. Инлайновый скрипт в `<head>` ставит язык до первой отрисовки — нет мигания. Старые `/ru/` пути редиректятся. |
| **B-17** | Админка для кейсов | Sveltia CMS на `/admin` — git-based, GitHub OAuth без своего бэкенда. Правит кейсы EN+RU, переводы и конфиг сайта. |
| **B-14** | Мобильная адаптация | Три брейкпоинта (1024/720/380) вместо одного. Починены: наезжающий nav, `padding-left: 16.25rem` в услугах, `width: 75rem` в контактах, центрирование лося. Добавлены правила под `hover: none`. |
| **B-10** | Деплой | Был SFTP на Namecheap. Заменён целиком: Docker-образ → ghcr → Oracelk. См. B-20. |
| — | Editorial-индекс `/works` | Бенто заменено на типографский индекс: крупные строки, обложка следует за курсором с наклоном по скорости, оркестрованное появление. |
| — | Хвост кейса | `CaseOutro` (CTA + контакты + следующий проект) и `CaseTopNav` (быстрый выход над заголовком). Кнопка Home убрана как дубль логотипа. |
| — | Плавный скролл | Lenis заменён на SmoothScroll.js: не владеет `scrollY`, поэтому средняя кнопка мыши, драг скроллбара и `scrollIntoView` работают нативно. Оформлено скиллом `smooth-wheel-scroll`. |
| — | Контейнеризация | Multi-stage образ, nginx на :8000, медиа отдельным слоем, smoke-тест в CI. См. [status.md](status.md). |
